#include <stdio.h>

int main()
{
    char chr;
    printf("enter a character");
    chr=getchar();
    if(chr=='A')
      printf("the character is A");
    else
      printf("the character is not A");
}
