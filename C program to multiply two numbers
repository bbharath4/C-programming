#include <stdio.h>
int main()
{
    double a,b,c;
    printf("enter the values of a and b:");
    scanf("%lf %lf",&a,&b);
    c=a*b;
    printf("the product of two numbers are:%lf",c);
    return 0;
}
